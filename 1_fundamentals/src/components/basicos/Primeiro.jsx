import React from "react";
//Componente baseado em funcoes
export default function Primeiro() {
  const msg = "Seja bem vindo!";

  return (
    <div>
      <h1>Primeiro Componente.</h1>
      <p>{msg}</p>
    </div>
  );
}
